{
  "name": "rodabmed",
  "private": true,
  "version": "1.0.0",
  "type": "module",
  "scripts": {
    "dev": "cd client && npm run dev",
    "build": "cd client && npm run build",
    "vercel-build": "cd client && npm run build"
  }
}
